spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movie
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.movie.reservation
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080
