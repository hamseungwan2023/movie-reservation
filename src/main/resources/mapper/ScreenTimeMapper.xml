<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.reservation.screentime.mapper.ScreenTimeMapper">
    <insert id="insert" parameterType="com.movie.reservation.screentime.ScreenTime" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO screen_time(start_time, end_time, screen_id, movie_id)
        VALUES (#{startTime}, #{endTime}, #{screenId}, #{movieId})
    </insert>

    <select id="findById" resultType="com.movie.reservation.screentime.ScreenTime">
        SELECT * FROM screen_time WHERE id = #{id}
    </select>

    <select id="findByMovieId" resultType="com.movie.reservation.screentime.ScreenTime">
        SELECT * FROM screen_time WHERE movie_id = #{movieId} ORDER BY id DESC
    </select>

    <select id="findByScreenId" resultType="com.movie.reservation.screentime.ScreenTime">
        SELECT * FROM screen_time WHERE screen_id = #{screenId} ORDER BY id DESC
    </select>

    <update id="update" parameterType="com.movie.reservation.screentime.ScreenTime">
        UPDATE screen_time
        SET start_time=#{startTime}, end_time=#{endTime}, screen_id=#{screenId}, movie_id=#{movieId}
        WHERE id=#{id}
    </update>

    <delete id="delete">
        DELETE FROM screen_time WHERE id=#{id}
    </delete>
</mapper>
